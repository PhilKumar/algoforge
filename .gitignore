# Environment variables - NEVER commit credentials!
.env
.env.staging
.env.local
.env.*.local

# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
env/
venv/
ENV/
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
lib/
lib64/
parts/
sdist/
var/
wheels/
*.egg-info/
.installed.cfg
*.egg
MANIFEST

# Virtual environments
.venv
venv/
ENV/

# IDE
.vscode/
.idea/
*.swp
*.swo
*~
.DS_Store

# User data — don't commit trade data to GitHub
strategies.json
runs.json

# Scrip master cache (downloaded daily, ~5MB)
.scrip_cache/

# SSH keys — NEVER commit
*.pem
*.key

# Credentials backup
*.env.backup
phillip_aws_accessKeys.csv
server.log
*.csv
*.log

# Runtime / temp files
.sessions.json
live_state_*.json
paper_state_*.json
trade_history.json
philforge.db
philforge.db-wal
philforge.db-shm
philforge.db*.bak*
.current_token
.token_lock
*.tmp
*.log
Daily Charts/

# Root-level study/media imports belong under static/notebooklm/
/*.m4a
/*.mp4
/*.pdf
/*.png

# SQLite database (per-server, contains user data)
philforge.db
philforge.db-shm
philforge.db-wal
test_philforge.db

# Study asset privacy manifest
static/notebooklm/.study-sanitize-index.json
tools/.nifty_cache/
tools/.upstox_cache/
fib_space_matrix_results.json

# The tearsheet generator is tracked; its editor backups are not.
tools/tearsheet/*.bak

# Sanctuary photos and per-user uploads live outside git
data/users/

# Personal bank documents dropped in for the sanctuary importer — NEVER tracked.
# The folder holds Aadhaar, signed loan agreements and card statements.
HDFC_Loan_Doc/
bank_statements/
payslips/
LifePapers/
WorkArchive/

# Data pulled off prod for safekeeping. Real trading history: never in git.
backups/
